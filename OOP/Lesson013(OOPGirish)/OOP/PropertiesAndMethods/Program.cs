namespace PropertiesAndMethods
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //Calulate calulate = new Calulate();
            //var netice = calulate.Sum(b: 15, a:85);//non-trailing named argument(C# 7.2)
            //Console.WriteLine(netice);
            ///----------------------------------------------------
            //Calulate calulate = new Calulate();

            //int[] reqemler = { 4, 5, 8, 11, 12 };
            //calulate.ArraySumElement(arrayLength: 200, 1,2,4,45,85,71,28,49);

            //---------------------------
            //Calulate calulate = new Calulate();
            //var netice = calulate.Factorial(5);

            //Console.WriteLine(netice);
        }
    }
}
